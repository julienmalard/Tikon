from tikon.Paisaje.PAISAJE import Paisaje


class Paisaje(Paisaje):

    def inic(தன், datos):
        return super().inic(datos=datos)

    def leer_documento(documento):
        return super().leer_documento()

    def simul(தன், fecha_init, tiempo_simul, paso):
        return super().simul(fecha_init=fecha_init, tiempo_simul=tiempo_simul, paso=paso)

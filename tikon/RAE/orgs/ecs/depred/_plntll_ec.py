from tikon.ecs.árb_mód import Ecuación


class EcuaciónDepred(Ecuación):
    def dens_depred(símismo):
        return dens[:, :, :, í_etps]

    def eval(símismo, paso):
        raise NotImplementedError